# Base URL of the backend API. In local dev, leave unset — Vite proxies /api
# to http://localhost:5000 (see vite.config.js). Set this for production builds.
# VITE_API_URL=https://your-domain.example.com/api
